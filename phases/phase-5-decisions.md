# phase-5-decisions.md — Autonomous adjudication (the chair decides)

Per PLAN.md: adjudicate the union autonomously, log rationale, proceed. Binding specs win over
reviewers; GOAL non-negotiables win over all. Convergence across ≥2 independent reviewers weighted
heavily. No human menu.

| # | id | disposition | decision + rationale |
|---|----|-------------|----------------------|
| 1 | test-firewall | **ADOPT** | Make the firewall STRUCTURAL: `[data-surface="test"]` CSS forces ring fill→track color, sets burst/sweep keyframes to `none`, neutralizes streak dots — a forgotten prop cannot leak a reward (CLAUDE.md §13 "hardcoded, not intended"). Codify the §8.3 distinction in a comment: an *inactive/muted* progress instrument is permitted; *active* rings/points/streaks/feedback/bursts are banned. The `<Momentum>` streak never renders under `data-surface="test"`. |
| 2 | focus-token-scope | **ADOPT** | Comprehensive `[data-surface="focus"]` re-scope of EVERY semantic token lesson descendants consume: canvas/surface/inset/hover/selected/track/chip, full ink ramp, border/border-strong/axis, accent+hover+tint, all `status-*`, success/error bg+border+ink tint pairs, focus-outline. Mechanism PROVEN (mr-gates Tailwind-v4 repro: utilities emit `var(--color-*)`; re-scope block kept UNLAYERED so it beats `@layer theme` `:root`). Comment forbids "tidying" it into a layer. |
| 3 | contrast-token-fails | **ADOPT** | Split text vs UI-only and darken failing on-light TEXT tokens to clear 4.5: mastery/success text → darker green (~#0F7A40), error text → darker (~#BE2F4B / keep current #b4543f passes), retrieval text → #B45309 (bright #D97706 = UI/icon/large only). `border-meaningful` darken to reach ≥3:1 on white. Dark: it's the BORDER (`canvas-border`) that must hit ≥3:1 vs canvas, not the raised fill — lighten canvas-border to ~#475569+. The audit script (below) PROVES every chosen value; build fails otherwise. |
| 4 | contrast-script-coverage | **ADOPT** | `scripts/contrast-audit.mjs` parses the actual tokens out of `globals.css` (light + each `[data-surface]` block) and checks an explicit manifest: ink/muted as text on each canvas; every action+status color as text on its surface (≥4.5) AND as UI (≥3); highlighters on dark; meaningful/axis borders ≥3; focus-outline vs canvas ≥3; raised-border vs canvas ≥3. Non-zero exit on any fail. |
| 5 | zero-churn-shells | **ADOPT (low-risk)** | Inventory: AppShell(children, displayName, workingAs) · StaffShell(children) · ParentShell(children, parentName). All three render the SAME 60px header skeleton today → factor a `Chrome({role, eyebrow, nav, identity, mode})` and keep the three as thin adapters with identical signatures. NavLink stays the only client island (mr-gates confirmed the boundary). |
| 6 | focussurface-bleed | **ADOPT** | Drop the negative-margin hack. Give `AppShell` a `mainSurface?: "trust"\|"focus"` prop that sets `data-surface` + drops `<main>` padding (`px-0 pt-0`) and paints `bg-canvas` when focus; the lesson page passes it. Self-contained, no sibling-padding coupling. |
| 7 | practice-surface | **SUBTRACT dark practice** | Spec §1 lists Focus = lessons+videos only; pee-wee (UX gate) + codex agree practice's dense feedback moment belongs on Trust. **learn/[skillId] = Focus dark; practice/[skillId] stays Trust (light)**, rewards present (not a gated test) but calm. Simpler + reversible + spec-aligned. Full re-scope (#2) still needed for the lesson surface. |
| 8 | momentum-ring-conflict | **ADOPT** | `RingTrio` (Focus/Mastery/Retrieval, §3/§12) is THE signature instrument on the learner dashboard; the existing course-mastery/XP/time-given-back become secondary stat cards (no longer styled as a competing "trio"). Wire RingTrio off best-available daily data; Mastery ring carries the gold cap at a node lock. |
| 9 | baseball-not-wired | **ADOPT (bounded)** | Wire `strikeZone` through `ProblemVisual`→`CoordinatePlane`; turn it ON for coordinate/slope visuals so it actually renders on a real lesson. Add `StatPanel` (Plex-Mono TrackMan readout) to the lesson chrome for slope/rate context. One structural instrument, not just trim. |
| 10 | training-boost | **DEFER → Phase 8 (justified)** | PLAN.md assigns "Training/Boost mode default-by-age + switchable" to **Phase 8** (Engagement guardrails + motivation) explicitly. Phase 5 ships the persistent MODE INDICATOR scaffold in the chrome; the switchable reward-intensity preference lands in Phase 8. Not a silent omission — roadmap-owned. |
| 11 | gold-cap-geometry | **ADOPT** | Cap = short arc at the terminus of the filled arc, rendered only when `goldCap` (node locked, value≈1). Reduced-motion → static gold cap + number, no sweep. |
| 12 | focus-ring-dark | **ADOPT** | Re-scope `:focus-visible` outline to `--color-focus` (#3D7BF2) under `[data-surface="focus"]`; add focus-outline-vs-canvas (≥3:1) to the audit. |
| 13 | reward-throttle-state | **ADOPT (light)** | Burst throttle is component-local per session (a timestamp ref); simultaneous locks → one burst; reduced-motion → static. No persistence layer in v1 (over-engineering for the gate). |
| 14 | ring-component-tests | **ADOPT** | Unit tests: gold cap renders only when `goldCap`; `muted` suppresses fill + sets inactive ARIA; Retrieval stroke is dashed; clamp01 edges; preserve ProgressRing butt-cap-at-0. |
| 15 | audit-build-hook | **ADOPT** | Add `"prebuild": "node scripts/contrast-audit.mjs"` to package.json + include in the check script so the gate actually runs. |
| 16 | chrome-scope-boundary | **ADOPT (note)** | Onboarding/auth/dev keep their own chrome and are Trust by default; in scope for token coherence (they inherit `:root` tokens + new fonts), out of scope for the Chrome refactor. Logged. |

**Rebuttals:** none stand — the two "unproven premise" concerns (Tailwind var-backing #2;
zero-churn #5) were empirically resolved by mr-gates, so they convert to ADOPT-with-guardrail, not
rebut. **No blocking concern is left deferred** → loop converges without a human gate, per PLAN.md
autonomous-adjudication mandate. Proceed to plan v2 + execute.
