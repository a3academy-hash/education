# Curriculum review notes (mr-kahn riders — binding for future authoring)

## ALG-E14 problem-bank authoring rider (mr-kahn, 2026-06-10)
Every ALG-E14 context hook is a same-rate scenario, so all sport on-ramps resolve
to identity or contradiction. When authoring p1/p2 banks for this node, MUST
interleave one-solution items (different rates — "the chase ends in week g"),
or students learn the surface cue "sport problem → terms cancel," defeating the
classification objective and corrupting misconception-tag diagnostics.

## Deferred to the CCSS verification pass (pre-accreditation)
- ALG-F02 football hook is vague ("fraction reasoning" with no actual problem).
- ALG-F03 volleyball "25-ish rallies" is mushy.
- ALG-E11 baseball hook misstates the strike zone as a fixed 18–42 inches
  (it is batter-relative).
- Full CCSS code verification across all 74 nodes (schema.notes flags this).

## Phase 1 carry-over (adaptive-router) — RESOLVED (Phase 1)
- RESOLVED: the router now classifies "remediate" only when the candidate
  blocks ≥ 1 dependent AND there is attempt evidence of the gap (candidate or
  any dependent has attempts > 0); a never-attempted frontier root (e.g.
  ALG-F01 with empty state) yields "continue".
